# TO DO

# given a weather, manipulate a node's edge max speed
# if a weather is storm, close edge (edge has an open bool variable)

from enum import Enum

class WeatherCondition(Enum):
    SUNNY = 0
    RAINY = 1
    SNOWY = 2
    STORM = 3

#class Weather:
